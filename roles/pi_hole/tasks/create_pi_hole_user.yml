- name: Create pihole group
  become: true
  ansible.builtin.group:
    gid: "{{ uid }}"
    name: pihole
    state: present

- name: Create pihole user
  become: true
  ansible.builtin.user:
    uid: "{{ uid }}"
    name: pihole
    shell: /bin/bash
    group: pihole
    state: present
