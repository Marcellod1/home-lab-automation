services:
  homepage:
    image: ghcr.io/gethomepage/homepage:{{ homepage_container_version }}
    container_name: {{ homepage_container_name }}
    ports:
      - {{ homepage_admin_port }}:3000
    volumes:
      - {{ docker_projects_directory }}/homepage/config:/app/config
    environment:
      PUID: {{ homepage_uid }}
      PGID: {{ homepage_gid }}
    restart: unless-stopped
